# 2026-04-03

## 完成工作

- 熟悉项目：AV资源聚合搜索下载器（Python Flask + 多源爬虫）
- 创建 `.gitignore`（排除 `__pycache__`、`*.log`、`*.html`、`.env` 等）
- 执行首次 git commit（root-commit `3692df4`），提交 17 个文件，921 行代码

## 项目概况

- **技术栈**：Python 3.12 + Flask + BeautifulSoup4 + requests
- **爬虫引擎**：JAVBus、JAVDB、Sukebei（engine/ 目录）
- **核心功能**：多源聚合搜索、智能排序（中文字幕优先）、磁力下载、Aria2 RPC
- **前端**：暗色模式 + 毛玻璃效果 + i18n，运行在 5002 端口
- **配置**：`config.py` + `.env` 可选覆盖，代理默认 `127.0.0.1:7890`
